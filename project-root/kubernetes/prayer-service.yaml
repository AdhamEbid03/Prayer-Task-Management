apiVersion: apps/v1
kind: Deployment
metadata:
  name: prayer-service
  labels:
    app: prayer-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prayer-service
  template:
    metadata:
      labels:
        app: prayer-service
    spec:
      containers:
      - name: prayer-service
        image: adham/prayer-service:latest
        ports:
        - containerPort: 3000
        env:
        - name: MONGO_URI
          value: mongodb://mongodb:27017/prayerdb
---
apiVersion: v1
kind: Service
metadata:
  name: prayer-service
spec:
  selector:
    app: prayer-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3000
  type: ClusterIP
